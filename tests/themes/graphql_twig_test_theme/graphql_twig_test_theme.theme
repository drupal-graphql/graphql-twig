<?php

/**
 * @file
 * Theme hook implementations.
 */

/**
 * Implements hook_theme().
 */
function graphql_twig_test_theme_theme($existing, $type, $theme, $path) {
  return [
    'graphql_echo' => [
      'variables' => ['input' => ''],
    ],
    'graphql_garage' => [
      'variables' => [],
    ],
  ];
}
